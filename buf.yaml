version: v1

deps:
  - buf.build/googleapis/googleapis

build:
  excludes:
    - node
    - java

breaking:
  use:
    - FILE

lint:
  use:
    - DEFAULT
  except:
    - RPC_RESPONSE_STANDARD_NAME
    - ENUM_VALUE_PREFIX
  allow_comment_ignores: true
