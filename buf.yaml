version: v2

modules:
  - path: .
    name: buf.build/stroeer/tapir
    excludes:
      - java
      - node
      - node-proto
      - node-buf

lint:
  use:
    - STANDARD
  except:
    - ENUM_VALUE_PREFIX
    - RPC_RESPONSE_STANDARD_NAME

breaking:
  use:
    - FILE
